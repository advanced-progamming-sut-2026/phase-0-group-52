package model.quest;

public enum RewardType {
    COIN,         // سکه (Currency)
    GEM,          // الماس (Currency)
    SEED_PACKET,  // بسته‌بذر (Inventory)
    PLANT_UNLOCK  // بازکردنِ گیاه (Unlockable)
}
